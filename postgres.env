# postgres.env

# กำหนดชื่อผู้ใช้
POSTGRES_USER=
# กำหนดรหัสผ่าน
POSTGRES_PASSWORD=
# กำหนดชื่อ Database
POSTGRES_DB=
# (ตัวเลือกเสริม) กำหนดตำแหน่งเก็บข้อมูลภายใน Container
PGDATA=/var/lib/postgresql/data/pgdata
