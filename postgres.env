# postgres.env

# กำหนดชื่อผู้ใช้
POSTGRES_USER=ken
# กำหนดรหัสผ่าน
POSTGRES_PASSWORD=3111ken!
# กำหนดชื่อ Database
POSTGRES_DB=bp_dashboard_db
# (ตัวเลือกเสริม) กำหนดตำแหน่งเก็บข้อมูลภายใน Container
PGDATA=/var/lib/postgresql/data/pgdata